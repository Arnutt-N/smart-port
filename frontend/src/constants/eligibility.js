/**
 * Canonical near-threshold windows — must stay aligned with CONTEXT.md
 * and backend/QualificationEngine::NEAR_THRESHOLD_DAYS (candidates = 90).
 */
export const CANDIDATE_NEAR_THRESHOLD_DAYS = 90

/** Probation UX window — intentionally different from candidates (CONTEXT.md). */
export const PROBATION_NEAR_THRESHOLD_DAYS = 30
