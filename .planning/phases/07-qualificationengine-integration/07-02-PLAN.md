---
phase: 07-qualificationengine-integration
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - frontend/src/composables/useCandidates.js
  - frontend/src/pages/CandidateListsPage.vue
autonomous: true
requirements: [QE-04]

must_haves:
  truths:
    - "Candidate list table shows supportive days column with number or dash"
    - "Candidate list table shows diverse status badge for M1 candidates, dash for others"
    - "Candidate list table shows equivalence days column with number or dash"
    - "Qualification date displayed is the adjusted value (already computed by engine)"
    - "Empty state row spans all 12 columns correctly"
  artifacts:
    - path: "frontend/src/composables/useCandidates.js"
      provides: "Extended mapCandidateRow with supportiveDays, equivalenceDays, diverseStatus"
      contains: "supportiveDays"
    - path: "frontend/src/pages/CandidateListsPage.vue"
      provides: "Table with 3 new columns for supportive, diverse, equivalence data"
      contains: "วันเกื้อกูล"
  key_links:
    - from: "frontend/src/composables/useCandidates.js"
      to: "backend API response"
      via: "mapCandidateRow snake_case to camelCase"
      pattern: "supportiveDays.*supportive_days"
    - from: "frontend/src/pages/CandidateListsPage.vue"
      to: "StatusBadge component"
      via: "diverseStatus prop"
      pattern: "StatusBadge.*diverseStatus"
---

<objective>
Update the Candidate List frontend to display the 3 new data fields computed by the engine: supportive days, diverse experience status badge (M1 only), and equivalence days.

Purpose: HR users see the adjusted qualification dates and the contributing factors directly in the candidate list table, without needing to navigate elsewhere.

Output: Updated useCandidates.js composable and CandidateListsPage.vue with 3 new table columns.
</objective>

<execution_context>
@D:/hrProject/smart-port/.claude/get-shit-done/workflows/execute-plan.md
@D:/hrProject/smart-port/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-qualificationengine-integration/07-CONTEXT.md
@.planning/phases/07-qualificationengine-integration/07-01-SUMMARY.md

<interfaces>
<!-- Backend response fields from Plan 01 -->
New fields returned by QualificationEngine for each candidate row:
- supportive_days (int) -- total effective days from supportive experience, 0 if none
- equivalence_days (int) -- total approved days from position equivalence, 0 if none
- diverse_diff_count (int) -- max diff_count from diverse experience, 0 if none
- diverse_status (string|null) -- 'DIFF_PASS' or 'DIFF_NOT_YET' for M1; null for other levels

From frontend/src/composables/useCandidates.js:
```javascript
function mapCandidateRow(row) {
    return {
        personnelId: row.personnel_id,
        name: row.full_name,
        currentPosition: row.current_position,
        currentLevelCode: row.current_level_code,
        currentLevelName: row.current_level_name,
        levelStartDate: row.level_start_date_thai,
        qualificationDate: row.qualification_date_thai,
        remainingDays: row.remaining_days,
        status: row.status,
        department: row.department,
    }
}
```

From frontend/src/components/StatusBadge.vue (confirmed keys):
  DIFF_PASS: { label: 'ผ่านเกณฑ์', class: 'bg-green-50 text-green-700' }
  DIFF_NOT_YET: { label: 'ยังไม่ครบ', class: 'bg-amber-50 text-amber-700' }

Current CandidateListsPage.vue table columns (9 total):
  ลำดับ | ชื่อ-นามสกุล | ตำแหน่งปัจจุบัน | ระดับตำแหน่ง | วันเข้าสู่ระดับปัจจุบัน | วันที่ครบกำหนด | จำนวนวันที่เหลือ | สถานะ | การดำเนินการ

Empty state row: colspan="9" -- must be updated to colspan="12"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend mapCandidateRow and add 3 columns to candidate list table</name>
  <files>frontend/src/composables/useCandidates.js, frontend/src/pages/CandidateListsPage.vue</files>
  <read_first>
    - frontend/src/composables/useCandidates.js
    - frontend/src/pages/CandidateListsPage.vue
    - frontend/src/components/StatusBadge.vue
  </read_first>
  <action>
**Part A: Update useCandidates.js** (per D-12)

Add 3 new fields to the mapCandidateRow function return object, after the `department` field:

```javascript
supportiveDays: row.supportive_days,
equivalenceDays: row.equivalence_days,
diverseStatus: row.diverse_status,  // 'DIFF_PASS', 'DIFF_NOT_YET', or null
```

**Part B: Update CandidateListsPage.vue table** (per D-12, D-13, D-14, D-15)

1. Add 3 new `<th>` headers in the thead, after "วันที่ครบกำหนด" and before "จำนวนวันที่เหลือ":

```html
<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">วันเกื้อกูล</th>
<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">สถานะ 3 ต่าง</th>
<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">วันเทียบ ตน.</th>
```

2. Add 3 new `<td>` cells in the tbody row, after the qualificationDate cell and before the remainingDays cell:

For supportive days (per D-13):
```html
<td class="px-6 py-3 text-gray-500">
  {{ row.supportiveDays > 0 ? `${row.supportiveDays} วัน` : '-' }}
</td>
```

For diverse status badge (per D-06, D-07, D-08 -- M1 only, null for other levels):
```html
<td class="px-6 py-3">
  <StatusBadge v-if="row.diverseStatus" :status="row.diverseStatus" />
  <span v-else class="text-gray-400">-</span>
</td>
```

For equivalence days (per D-14):
```html
<td class="px-6 py-3 text-gray-500">
  {{ row.equivalenceDays > 0 ? `${row.equivalenceDays} วัน` : '-' }}
</td>
```

3. Update the empty state row colspan from `9` to `12`:
```html
<td colspan="12">
```

Note: The qualification_date displayed is already the adjusted value from the engine (per D-15). No frontend formula needed -- just display qualification_date_thai as before.
  </action>
  <verify>
    <automated>cd D:/hrProject/smart-port/frontend && npx vue-tsc --noEmit 2>&1 || echo "vue-tsc not available, checking with grep" && grep -c "supportiveDays" src/composables/useCandidates.js && grep -c "วันเกื้อกูล" src/pages/CandidateListsPage.vue && grep -c "diverseStatus" src/pages/CandidateListsPage.vue && grep 'colspan="12"' src/pages/CandidateListsPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep "supportiveDays" frontend/src/composables/useCandidates.js matches
    - grep "equivalenceDays" frontend/src/composables/useCandidates.js matches
    - grep "diverseStatus" frontend/src/composables/useCandidates.js matches
    - grep "วันเกื้อกูล" frontend/src/pages/CandidateListsPage.vue matches
    - grep "สถานะ 3 ต่าง" frontend/src/pages/CandidateListsPage.vue matches
    - grep "วันเทียบ ตน." frontend/src/pages/CandidateListsPage.vue matches
    - grep "StatusBadge.*diverseStatus" frontend/src/pages/CandidateListsPage.vue matches
    - grep 'colspan="12"' frontend/src/pages/CandidateListsPage.vue matches (updated from 9)
    - grep 'colspan="9"' frontend/src/pages/CandidateListsPage.vue does NOT match (old value removed)
  </acceptance_criteria>
  <done>useCandidates.js maps 3 new fields; CandidateListsPage.vue has 12 columns total (9 existing + 3 new); supportive/equivalence show number or dash; diverse shows StatusBadge or dash; empty state colspan updated</done>
</task>

</tasks>

<verification>
1. `npm run build` in frontend/ -- no build errors
2. If dev server running: navigate to candidate list page, verify 12 column headers visible
3. For K2/K3 tabs: new columns show "-" (no supportive/diverse/equivalence data expected)
4. Diverse status column shows "-" for all non-M1 levels (correct behavior since M1 not in validTargets yet)
</verification>

<success_criteria>
- Candidate list table has 12 columns (9 original + 3 new: วันเกื้อกูล, สถานะ 3 ต่าง, วันเทียบ ตน.)
- Supportive days shows "{N} วัน" or "-" (per D-13)
- Diverse status shows StatusBadge (DIFF_PASS/DIFF_NOT_YET) or "-" (per D-06, D-07)
- Equivalence days shows "{N} วัน" or "-" (per D-14)
- Qualification date column displays adjusted value from engine (per D-15)
- Empty state colspan = 12
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-qualificationengine-integration/07-02-SUMMARY.md`
</output>
