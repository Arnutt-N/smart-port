---
phase: 04-database-preparation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/08-career-path-v11.sql
  - docker-compose.yaml
autonomous: true
requirements:
  - SE-01
must_haves:
  truths:
    - "supportive_job_series table contains seed data with at least 13 directional mapping pairs"
    - "Each seed row has a ratio_percent value (integer, default 100)"
    - "diff_count column in diverse_experience is a STORED GENERATED column computed from 4 boolean flags"
    - "Docker compose initializes the new SQL file on fresh database creation"
  artifacts:
    - path: "database/08-career-path-v11.sql"
      provides: "ALTER TABLE statements + seed INSERT for supportive_job_series"
      contains: "SET NAMES utf8mb4"
    - path: "docker-compose.yaml"
      provides: "Volume mount for 08-career-path-v11.sql"
      contains: "08-career-path-v11.sql"
  key_links:
    - from: "database/08-career-path-v11.sql"
      to: "docker-compose.yaml"
      via: "volume mount in db service"
      pattern: "08-career-path-v11.sql:/docker-entrypoint-initdb.d/"
    - from: "database/08-career-path-v11.sql"
      to: "database/04-career-path.sql"
      via: "ALTER TABLE on tables created by 04"
      pattern: "ALTER TABLE supportive_job_series"
---

<objective>
Create the v1.1 database migration SQL file containing schema refinements (ratio_percent column, GENERATED diff_count column) and seed data for supportive_job_series mappings, then wire it into Docker compose.

Purpose: Prepares the data layer so Phase 5 backend CRUD APIs have the correct schema and reference data to work against.
Output: `database/08-career-path-v11.sql` with ALTER TABLE + seed INSERT, `docker-compose.yaml` updated with volume mount.
</objective>

<execution_context>
@D:/hrProject/smart-port/.claude/get-shit-done/workflows/execute-plan.md
@D:/hrProject/smart-port/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-database-preparation/04-CONTEXT.md
@.planning/phases/04-database-preparation/04-RESEARCH.md

<interfaces>
<!-- Existing table schemas the executor must understand -->

From database/04-career-path.sql — supportive_job_series (lines 182-194):
```sql
CREATE TABLE supportive_job_series (
    mapping_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    primary_series_id BIGINT,
    primary_series_name VARCHAR(200) NOT NULL,
    supportive_series_id BIGINT,
    supportive_series_name VARCHAR(200) NOT NULL,
    mapping_type VARCHAR(50) DEFAULT 'SAME_GROUP',  -- SAME_GROUP, SPECIFIC, EXCLUSIVE
    is_active TINYINT(1) DEFAULT 1,
    effective_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (primary_series_id) REFERENCES lookup_value(value_id),
    FOREIGN KEY (supportive_series_id) REFERENCES lookup_value(value_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```
NOTE: This table has NO ratio_percent column. Must be added via ALTER TABLE.

From database/04-career-path.sql — diverse_experience diff_count (line 71):
```sql
    diff_count INT DEFAULT 0,                      -- จำนวนต่าง (0-4)
```
NOTE: This is a plain INT with DEFAULT 0. Must be converted to STORED GENERATED.

From docker-compose.yaml — db volumes pattern (lines 53-60):
```yaml
    volumes:
      - db-data:/var/lib/mysql
      - ./mysql_database_design.sql:/docker-entrypoint-initdb.d/01-schema.sql
      ...
      - ./database/07-add-education-level.sql:/docker-entrypoint-initdb.d/07-add-education-level.sql
```

From database/06-seed-data.sql — seed data pattern (line 1):
```sql
SET NAMES utf8mb4;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 08-career-path-v11.sql with ALTER TABLEs and seed data</name>
  <files>database/08-career-path-v11.sql</files>
  <read_first>
    database/04-career-path.sql
    database/06-seed-data.sql
    database/07-add-education-level.sql
    .planning/phases/04-database-preparation/04-RESEARCH.md
  </read_first>
  <action>
Create `database/08-career-path-v11.sql` with the following exact content structure:

**Header (per D-04):**
```sql
SET NAMES utf8mb4;
-- ============================================================================
-- 08-career-path-v11.sql
-- v1.1 Database Preparation: ALTER TABLEs + Seed Data
-- สำนักงานปลัดกระทรวงยุติธรรม
--
-- Contents:
--   Section 1: ALTER supportive_job_series (add ratio_percent)
--   Section 2: ALTER diverse_experience (diff_count -> GENERATED)
--   Section 3: Seed supportive_job_series mapping data
--
-- Dependencies: 04-career-path.sql (tables must exist)
-- NOTE: Docker MySQL only runs init scripts on first volume creation.
--       After adding this file, run: docker-compose down -v && docker-compose up
-- ============================================================================
```

**Section 1 — ALTER supportive_job_series (per D-08):**
Add `ratio_percent` column AFTER `mapping_type`:
```sql
ALTER TABLE supportive_job_series
  ADD COLUMN ratio_percent INT DEFAULT 100
  AFTER mapping_type;
```

**Section 2 — ALTER diverse_experience (per D-05):**
Convert `diff_count` from plain INT DEFAULT 0 to STORED GENERATED column. Use MODIFY approach first (MySQL 8.0 supports non-generated to stored generated). The table is empty so no data loss risk.
```sql
ALTER TABLE diverse_experience
  MODIFY COLUMN diff_count INT
  GENERATED ALWAYS AS (is_diff_job_series + is_diff_org + is_diff_location + is_diff_work_nature) STORED;
```

**Section 3 — Seed data (per D-01, D-02, D-09):**
Insert the following 14 directional mapping rows. Each mapping is directional per D-09 (A supports B does NOT mean B supports A). Use `primary_series_id = NULL` and `supportive_series_id = NULL` (FK columns reference lookup_value which may not have matching IDs — name-based matching is the primary lookup mechanism per research anti-patterns). Set `effective_date = '2024-03-22'` and `is_active = 1` for all rows.

The confirmed mappings from the gap analysis SQL comments:

| primary_series_name | supportive_series_name | mapping_type | ratio_percent |
|---|---|---|---|
| นักประชาสัมพันธ์ | นักวิชาการโสตทัศนศึกษา | SAME_GROUP | 100 |
| นักวิเคราะห์นโยบายและแผน | นักวิชาการยุติธรรม | SAME_GROUP | 100 |
| นักวิเคราะห์นโยบายและแผน | นักทรัพยากรบุคคล | SAME_GROUP | 100 |
| นักวิเคราะห์นโยบายและแผน | นักจัดการงานทั่วไป | SAME_GROUP | 100 |
| นักวิชาการยุติธรรม | นักวิเคราะห์นโยบายและแผน | SAME_GROUP | 100 |
| นักวิชาการยุติธรรม | นักทรัพยากรบุคคล | SAME_GROUP | 100 |
| นักวิชาการยุติธรรม | นักจัดการงานทั่วไป | SAME_GROUP | 100 |
| นักทรัพยากรบุคคล | นักวิเคราะห์นโยบายและแผน | SAME_GROUP | 100 |
| นักทรัพยากรบุคคล | นักวิชาการยุติธรรม | SAME_GROUP | 100 |
| นักทรัพยากรบุคคล | นักจัดการงานทั่วไป | SAME_GROUP | 100 |
| นักจัดการงานทั่วไป | นักวิเคราะห์นโยบายและแผน | SAME_GROUP | 100 |
| นักจัดการงานทั่วไป | นักวิชาการยุติธรรม | SAME_GROUP | 100 |
| นักจัดการงานทั่วไป | นักทรัพยากรบุคคล | SAME_GROUP | 100 |
| นิติกร | นิติกร | EXCLUSIVE | 100 |

Use a single multi-row INSERT statement:
```sql
INSERT INTO supportive_job_series
  (primary_series_name, supportive_series_name, mapping_type, ratio_percent, is_active, effective_date)
VALUES
  ('นักประชาสัมพันธ์', 'นักวิชาการโสตทัศนศึกษา', 'SAME_GROUP', 100, 1, '2024-03-22'),
  ...
```

Add a trailing comment documenting the gap:
```sql
-- NOTE: Additional supportive_job_series mappings (เจ้าพนักงานธุรการ O-series,
-- and full PDF pages 32-82 coverage) should be added by HR or via admin UI.
-- These 14 rows cover the K-series วิชาการ group confirmed from gap_analysis SQL comments.
```
  </action>
  <verify>
    <automated>grep -c "INSERT INTO supportive_job_series" database/08-career-path-v11.sql && grep "SET NAMES utf8mb4" database/08-career-path-v11.sql && grep "GENERATED ALWAYS AS" database/08-career-path-v11.sql && grep "ratio_percent" database/08-career-path-v11.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SET NAMES utf8mb4" database/08-career-path-v11.sql` returns 1
    - `grep "GENERATED ALWAYS AS" database/08-career-path-v11.sql` matches the diff_count expression
    - `grep "ADD COLUMN ratio_percent" database/08-career-path-v11.sql` matches ALTER TABLE statement
    - `grep -c "นักทรัพยากรบุคคล" database/08-career-path-v11.sql` returns at least 6 (appears in multiple seed rows)
    - `grep -c "นักวิเคราะห์นโยบายและแผน" database/08-career-path-v11.sql` returns at least 4
    - `grep "EXCLUSIVE" database/08-career-path-v11.sql` matches the นิติกร row
    - `grep "08-career-path-v11.sql" database/08-career-path-v11.sql` matches the header comment
    - File starts with SET NAMES utf8mb4 (first non-empty line)
  </acceptance_criteria>
  <done>
    SQL file exists at database/08-career-path-v11.sql containing: (1) ALTER TABLE supportive_job_series adding ratio_percent INT DEFAULT 100, (2) ALTER TABLE diverse_experience converting diff_count to STORED GENERATED column, (3) 14 seed INSERT rows for supportive_job_series with Thai series names and ratio_percent values. File starts with SET NAMES utf8mb4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire 08-career-path-v11.sql into Docker compose</name>
  <files>docker-compose.yaml</files>
  <read_first>
    docker-compose.yaml
    database/08-career-path-v11.sql
  </read_first>
  <action>
Edit `docker-compose.yaml` to add the new SQL init file volume mount in the `db` service `volumes` section. Add it AFTER the existing line for `07-add-education-level.sql` (currently line 60).

Add this line with the same indentation pattern as existing volume mounts:
```yaml
      - ./database/08-career-path-v11.sql:/docker-entrypoint-initdb.d/08-career-path-v11.sql
```

The line should be placed immediately after:
```yaml
      - ./database/07-add-education-level.sql:/docker-entrypoint-initdb.d/07-add-education-level.sql
```

Do NOT modify any other part of docker-compose.yaml. Preserve exact existing indentation (6 spaces before the dash).
  </action>
  <verify>
    <automated>grep "08-career-path-v11.sql" docker-compose.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "08-career-path-v11.sql:/docker-entrypoint-initdb.d/08-career-path-v11.sql" docker-compose.yaml` matches exactly one line
    - `grep -c "docker-entrypoint-initdb.d" docker-compose.yaml` returns 8 (was 7, now 8)
    - The new line appears after the 07-add-education-level.sql line (verify with grep -n)
  </acceptance_criteria>
  <done>
    docker-compose.yaml contains volume mount for 08-career-path-v11.sql, positioned after 07-add-education-level.sql, with correct path mapping to /docker-entrypoint-initdb.d/.
  </done>
</task>

</tasks>

<verification>
1. File exists: `ls database/08-career-path-v11.sql` succeeds
2. UTF-8 header: `head -1 database/08-career-path-v11.sql` shows `SET NAMES utf8mb4;`
3. ALTER TABLE for ratio_percent: `grep "ADD COLUMN ratio_percent" database/08-career-path-v11.sql`
4. GENERATED column: `grep "GENERATED ALWAYS AS (is_diff_job_series" database/08-career-path-v11.sql`
5. Seed data count: `grep -c "SAME_GROUP\|EXCLUSIVE" database/08-career-path-v11.sql` returns >= 14
6. Docker wiring: `grep "08-career-path-v11.sql" docker-compose.yaml` returns the volume mount line
7. Thai text present: `grep "นักทรัพยากรบุคคล" database/08-career-path-v11.sql` returns matches
</verification>

<success_criteria>
- database/08-career-path-v11.sql exists with correct SET NAMES utf8mb4 header
- ALTER TABLE adds ratio_percent to supportive_job_series
- ALTER TABLE converts diff_count to STORED GENERATED column
- At least 13 seed rows inserted into supportive_job_series with directional mappings
- docker-compose.yaml mounts the new SQL file into /docker-entrypoint-initdb.d/
- All Thai text is properly encoded (not mojibake)
</success_criteria>

<output>
After completion, create `.planning/phases/04-database-preparation/04-01-SUMMARY.md`
</output>
