---
phase: 06-frontend-crud-pages
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/pages/EquivalencePage.vue
autonomous: true
requirements: [PE-02]

must_haves:
  truths:
    - "HR can see a list of position equivalence records with approval status badges (PENDING/APPROVED/REJECTED)"
    - "HR can create a new equivalence request via modal form, which always creates with PENDING status"
    - "HR can edit PENDING records but not APPROVED or REJECTED records"
    - "HR can approve PENDING records via modal requiring approved_start_date and approved_end_date"
    - "HR can reject PENDING records via simple confirmation dialog"
    - "Action buttons vary by status: PENDING shows edit/approve/reject, APPROVED/REJECTED shows view-only"
  artifacts:
    - path: "frontend/src/pages/EquivalencePage.vue"
      provides: "Full CRUD page for position equivalence with approval workflow"
      min_lines: 280
  key_links:
    - from: "frontend/src/pages/EquivalencePage.vue"
      to: "frontend/src/composables/useEquivalence.js"
      via: "import { useEquivalence }"
      pattern: "useEquivalence"
    - from: "frontend/src/pages/EquivalencePage.vue"
      to: "frontend/src/components/StatusBadge.vue"
      via: "PENDING / APPROVED / REJECTED status"
      pattern: "PENDING\\|APPROVED\\|REJECTED"
---

<objective>
Create the Position Equivalence CRUD page (การเทียบตำแหน่ง) at /position-compare with list view showing approval status badges, modal create/edit forms, and approval/rejection workflow.

Purpose: HR can manage position equivalence requests with approval workflow -- create requests (always PENDING), approve with date range, or reject. Requirement PE-02.
Output: EquivalencePage.vue
</objective>

<execution_context>
@D:/hrProject/smart-port/.claude/get-shit-done/workflows/execute-plan.md
@D:/hrProject/smart-port/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-crud-pages/06-CONTEXT.md
@.planning/phases/06-frontend-crud-pages/06-RESEARCH.md
@.planning/phases/06-frontend-crud-pages/06-01-SUMMARY.md

<interfaces>
From frontend/src/composables/useEquivalence.js (created in Plan 01):
```javascript
export function useEquivalence() {
  return { fetchList, fetchDetail, create, update, approve, reject }
  // approve(id, { approvedStartDate, approvedEndDate }) -> calls PUT /equivalence/{id} with { approval_status: 'APPROVED', approved_start_date, approved_end_date }
  // reject(id) -> calls PUT /equivalence/{id} with { approval_status: 'REJECTED' }
  // Mapped row fields (camelCase):
  // equivalenceId, personnelId, fullName, actualPosition, equivalentType,
  // requestStartDate, requestEndDate, requestStartDateThai, requestEndDateThai,
  // requestTotalDays, approvalStatus, approvedStartDate, approvedEndDate,
  // approvedStartDateThai, approvedEndDateThai, approvedTotalDays,
  // approvedBy, approvedByName, approvalOrderRef
}
```

StatusBadge keys (added in Plan 01):
- PENDING: { label: 'รออนุมัติ', class: 'bg-amber-50 text-amber-700' }
- APPROVED: { label: 'อนุมัติแล้ว', class: 'bg-green-50 text-green-700' }
- REJECTED: { label: 'ไม่อนุมัติ', class: 'bg-red-50 text-red-700' }

POST /equivalence request body:
```json
{
  "personnel_id": 5,
  "actual_position": "เจ้าพนักงานธุรการ",
  "equivalent_type": "นักจัดการงานทั่วไป",
  "request_start_date": "2023-01-01",
  "request_end_date": "2024-12-31",
  "approval_order_ref": "คำสั่งที่ 123/2566"
}
```

PUT /equivalence/{id} approve body:
```json
{
  "approval_status": "APPROVED",
  "approved_start_date": "2023-01-01",
  "approved_end_date": "2024-12-31"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EquivalencePage.vue with approval workflow</name>
  <files>frontend/src/pages/EquivalencePage.vue</files>
  <read_first>
    frontend/src/pages/ProbationEndPage.vue,
    frontend/src/composables/useEquivalence.js,
    frontend/src/composables/useApi.js,
    frontend/src/components/StatusBadge.vue
  </read_first>
  <action>
Create `frontend/src/pages/EquivalencePage.vue` following ProbationEndPage layout (per D-01).

**Template structure (per D-01, D-18 through D-22):**

1. **Breadcrumb**: Home icon, "/", "การเทียบตำแหน่ง" (per D-25)

2. **Page header**: h1 "การเทียบตำแหน่ง", p "ยื่นคำขอเทียบตำแหน่งและติดตามสถานะอนุมัติ", Add button "ยื่นคำขอใหม่"

3. **Stat cards** (per D-02): 4 StatCard components:
   - "คำขอทั้งหมด" with `FileText` icon, blue bg, value = pagination.total
   - "รออนุมัติ" with `Clock` icon, amber bg, value = count where approvalStatus === 'PENDING'
   - "อนุมัติแล้ว" with `CheckCircle` icon, green bg, value = count where approvalStatus === 'APPROVED'
   - "ไม่อนุมัติ" with `XCircle` icon, red bg, value = count where approvalStatus === 'REJECTED'

4. **Search bar** (per D-03): Same debounced + IME guard pattern. Placeholder: "ค้นหาชื่อ หรือตำแหน่ง..."

5. **Loading/Error/Empty states** (per D-05)

6. **Data table** (per D-04, D-18, D-19, D-20):
   - Columns: ลำดับ | ชื่อ-สกุล | ตำแหน่งจริง | เทียบเป็น | วันที่ขอ | สถานะ | วันที่อนุมัติ | จัดการ
   - Row values:
     - ลำดับ: `pagination.offset + index + 1`
     - ชื่อ-สกุล: `row.fullName`
     - ตำแหน่งจริง: `row.actualPosition`
     - เทียบเป็น: `row.equivalentType`
     - วันที่ขอ: `row.requestStartDateThai + ' - ' + row.requestEndDateThai` and below it `row.requestTotalDays + ' วัน'` in small gray text
     - สถานะ: `<StatusBadge :status="row.approvalStatus" />` (per D-19)
     - วันที่อนุมัติ: If APPROVED, show `row.approvedStartDateThai + ' - ' + row.approvedEndDateThai` and `row.approvedTotalDays + ' วัน'`. If not APPROVED, show `'-'`
     - จัดการ: **Conditional action buttons per D-20:**

   ```html
   <!-- PENDING: edit + approve + reject buttons -->
   <template v-if="row.approvalStatus === 'PENDING'">
     <button @click="openEdit(row)" class="p-1 text-gray-400 hover:text-blue-600" title="แก้ไข">
       <Pencil class="w-4 h-4" />
     </button>
     <button @click="openApprove(row)" class="p-1 text-gray-400 hover:text-green-600" title="อนุมัติ">
       <Check class="w-4 h-4" />
     </button>
     <button @click="confirmReject(row.equivalenceId)" class="p-1 text-gray-400 hover:text-red-600" title="ไม่อนุมัติ">
       <X class="w-4 h-4" />
     </button>
   </template>
   <!-- APPROVED/REJECTED: view only -->
   <template v-else>
     <button @click="openView(row)" class="p-1 text-gray-400 hover:text-blue-600" title="ดูรายละเอียด">
       <Eye class="w-4 h-4" />
     </button>
   </template>
   ```

7. **PaginationBar**: Same pattern

8. **Create/Edit Modal** (per D-06, D-22):
   - Title: "ยื่นคำขอเทียบตำแหน่ง" (create, per D-22) or "แก้ไขคำขอ" (edit)
   - **No status selection in create form** (per D-22 -- POST always creates with PENDING)
   - Form fields:
     - **personnel_id**: Same autocomplete as other pages (per D-07). Disabled in edit mode.
     - **actual_position**: Label "ตำแหน่งจริง", text input, required
     - **equivalent_type**: Label "เทียบเป็นตำแหน่ง", text input, required
     - **request_start_date**: Label "วันเริ่มต้น (คำขอ)", date input, required
     - **request_end_date**: Label "วันสิ้นสุด (คำขอ)", date input, required
     - **approval_order_ref**: Label "อ้างอิงคำสั่ง", text input, optional
   - Validation (per D-10): Required = personnel_id, actual_position, equivalent_type, request_start_date, request_end_date. Red border + Thai error.
   - Footer: "ยกเลิก" + "บันทึก"

9. **Approve Modal** (per D-21):
   - Separate modal shown when `showApproveModal` ref is true
   - Title: "อนุมัติการเทียบตำแหน่ง"
   - Shows summary of the record being approved: fullName, actualPosition, equivalentType
   - Form fields:
     - **approved_start_date**: Label "วันเริ่มต้นที่อนุมัติ", `<input type="date">`, required. Pre-fill with request_start_date from the record.
     - **approved_end_date**: Label "วันสิ้นสุดที่อนุมัติ", `<input type="date">`, required. Pre-fill with request_end_date from the record.
   - Footer: "ยกเลิก" + "อนุมัติ" (green bg button, `@click="handleApprove"`)
   - `handleApprove()` calls `approve(approvingRecord.equivalenceId, { approvedStartDate: approveForm.approved_start_date, approvedEndDate: approveForm.approved_end_date })`, shows toast, fetchData()

10. **Reject Confirmation Dialog** (per D-21):
    - Simple confirmation modal: "ยืนยันการไม่อนุมัติ", "คุณต้องการไม่อนุมัติคำขอนี้หรือไม่?"
    - Buttons: "ยกเลิก" + "ยืนยัน" (red bg)
    - On confirm: calls `reject(rejectingId)`, shows toast, fetchData()

11. **View Modal** (for APPROVED/REJECTED records):
    - Read-only display of all record details
    - Shows approval info if APPROVED: approved dates, approved_total_days, approved_by_name
    - Close button only

**Script setup:**

```javascript
import { ref, onMounted } from 'vue'
import { useEquivalence } from '@/composables/useEquivalence.js'
import { useApi } from '@/composables/useApi.js'
import { useUiStore } from '@/stores/ui.js'
```

State refs:
- Standard: `loading`, `error`, `rows`, `pagination`, `searchQuery`, `isComposing`
- Create/Edit modal: `showModal`, `editingRecord`, `formData`, `formErrors`, `saving`
- Approve modal: `showApproveModal`, `approvingRecord`, `approveForm` (with approved_start_date, approved_end_date)
- Reject dialog: `showRejectConfirm`, `rejectingId`
- View modal: `showViewModal`, `viewingRecord`
- Autocomplete: `personnelSearch`, `personnelResults`, `showPersonnelDropdown`

formData initial shape:
```javascript
const defaultFormData = () => ({
  personnel_id: null,
  actual_position: '',
  equivalent_type: '',
  request_start_date: '',
  request_end_date: '',
  approval_order_ref: '',
})
```

Functions:
- `openApprove(record)` -- set approvingRecord, pre-fill approveForm with record's request dates, show modal
- `handleApprove()` -- validate dates, call approve(), toast, close, fetchData
- `confirmReject(id)` -- set rejectingId, show dialog
- `handleReject()` -- call reject(), toast, close, fetchData
- `openView(record)` -- set viewingRecord, show view modal
- Standard CRUD functions same as other pages

Toast messages (per D-27):
- Create: `showToast('ยื่นคำขอสำเร็จ', 'success')`
- Update: `showToast('อัปเดตสำเร็จ', 'success')`
- Approve: `showToast('อนุมัติสำเร็จ', 'success')`
- Reject: `showToast('ดำเนินการสำเร็จ', 'success')`
- Error: `showToast(err.message || 'เกิดข้อผิดพลาด', 'error')`
  </action>
  <verify>
    <automated>grep -c "การเทียบตำแหน่ง" frontend/src/pages/EquivalencePage.vue && grep -c "useEquivalence" frontend/src/pages/EquivalencePage.vue && grep -c "PENDING\|APPROVED\|REJECTED" frontend/src/pages/EquivalencePage.vue && grep -c "showApproveModal" frontend/src/pages/EquivalencePage.vue && grep -c "showRejectConfirm" frontend/src/pages/EquivalencePage.vue && grep -c "approved_start_date\|approvedStartDate" frontend/src/pages/EquivalencePage.vue && grep -c "isComposing" frontend/src/pages/EquivalencePage.vue</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/pages/EquivalencePage.vue exists and is >= 280 lines
    - grep "การเทียบตำแหน่ง" returns match (Thai page title per D-25)
    - grep "useEquivalence" returns match (composable import per D-23)
    - grep "PENDING" returns match (approval status per D-19)
    - grep "APPROVED" returns match (approval status per D-19)
    - grep "REJECTED" returns match (approval status per D-19)
    - grep "showApproveModal" returns match (approve modal per D-21)
    - grep "showRejectConfirm" returns match (reject dialog per D-21)
    - grep "approved_start_date\|approvedStartDate" returns match (approve form fields per D-21)
    - grep "approved_end_date\|approvedEndDate" returns match (approve form fields per D-21)
    - grep "approvalStatus === 'PENDING'" returns match (conditional buttons per D-20)
    - grep "Eye" returns match (view-only icon for non-PENDING records per D-20)
    - grep "showModal" returns match (create/edit modal per D-06)
    - grep "isComposing" returns match (Thai IME guard per D-03)
    - grep "civil-servants" returns match (personnel autocomplete per D-07)
    - grep "PaginationBar" returns match (per D-01)
    - grep "StatusBadge" returns match (status badges per D-19)
    - grep "showToast" returns match (per D-27)
    - No "approval_status" field in the create formData (per D-22 -- always PENDING)
  </acceptance_criteria>
  <done>EquivalencePage.vue exists with: list showing approval status badges, conditional action buttons (edit/approve/reject for PENDING, view-only for others), create modal without status field, approve modal with date inputs, reject confirmation, search with IME guard, pagination, Thai UI</done>
</task>

</tasks>

<verification>
- EquivalencePage.vue follows ProbationEndPage layout pattern
- All D-18 through D-22 decisions implemented
- Approval status badges use StatusBadge with PENDING/APPROVED/REJECTED
- Conditional action buttons based on approval_status
- Approve modal requires dates, reject uses simple confirmation
- Create form has no status field (always PENDING per D-22)
</verification>

<success_criteria>
- HR can view position equivalence records with approval status badges
- HR can create new equivalence requests (always PENDING)
- HR can edit PENDING records only
- HR can approve PENDING records with approved date range
- HR can reject PENDING records with confirmation
- APPROVED/REJECTED records show view-only button
- All text in Thai
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-crud-pages/06-04-SUMMARY.md`
</output>
