---
phase: 06-frontend-crud-pages
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/pages/SupportivePage.vue
autonomous: true
requirements: [SE-03]

must_haves:
  truths:
    - "HR can see a list of supportive experience records with personnel name, job series, dates in Buddhist Era, total_days, ratio, effective_days"
    - "HR can create a new supportive experience record via modal form with personnel autocomplete and job series input"
    - "HR can edit an existing supportive experience record via modal"
    - "HR can delete a supportive experience record with confirmation dialog"
    - "Page shows stat cards, supports search with Thai IME guard, and has pagination"
  artifacts:
    - path: "frontend/src/pages/SupportivePage.vue"
      provides: "Full CRUD page for supportive experience"
      min_lines: 200
  key_links:
    - from: "frontend/src/pages/SupportivePage.vue"
      to: "frontend/src/composables/useSupportive.js"
      via: "import { useSupportive }"
      pattern: "useSupportive"
    - from: "frontend/src/pages/SupportivePage.vue"
      to: "/civil-servants?search="
      via: "useApi().get for personnel autocomplete"
      pattern: "civil-servants.*search"
---

<objective>
Create the Supportive Experience CRUD page (การนับเกื้อกูล) at /time-counting with list view, modal create/edit forms, and delete confirmation.

Purpose: HR can manage supportive experience records -- view, create, edit, delete -- with personnel autocomplete and job series selection. Requirement SE-03.
Output: SupportivePage.vue
</objective>

<execution_context>
@D:/hrProject/smart-port/.claude/get-shit-done/workflows/execute-plan.md
@D:/hrProject/smart-port/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend-crud-pages/06-CONTEXT.md
@.planning/phases/06-frontend-crud-pages/06-RESEARCH.md
@.planning/phases/06-frontend-crud-pages/06-01-SUMMARY.md

<interfaces>
<!-- Composable created in Plan 01 -->
From frontend/src/composables/useSupportive.js:
```javascript
export function useSupportive() {
  // Returns:
  return { fetchList, fetchDetail, create, update, remove }
  // fetchList({ search, limit, offset }) -> { success, data: [mapped rows], pagination }
  // fetchDetail(id) -> { success, data: mapped row }
  // create(data) -> API response
  // update(id, data) -> API response
  // remove(id) -> API response
}
// Mapped row fields (camelCase):
// supportiveId, personnelId, fullName, jobSeriesName, primarySeriesName,
// startDate, endDate, startDateThai, endDateThai, totalDays, ratioPercent,
// effectiveDays, netEndDate, description
```

From frontend/src/composables/useApi.js:
```javascript
export function useApi() {
  return { get, post, put, del, upload }
}
```

From frontend/src/components (all existing, ready to use):
- StatCard: props { label, value, icon, iconBgClass, iconClass }
- StatusBadge: props { status }
- SkeletonLoader: props { type: 'table'|'stat-cards', rows }
- EmptyState: props { icon, title, description } + default slot
- PaginationBar: props { total, limit, offset }, emits { 'update:offset' }

ProbationEndPage.vue layout pattern (FOLLOW THIS EXACTLY per D-01):
breadcrumb -> page header -> stat cards -> search bar -> data table -> PaginationBar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SupportivePage.vue with full CRUD functionality</name>
  <files>frontend/src/pages/SupportivePage.vue</files>
  <read_first>
    frontend/src/pages/ProbationEndPage.vue,
    frontend/src/composables/useSupportive.js,
    frontend/src/composables/useApi.js,
    frontend/src/components/StatCard.vue,
    frontend/src/components/PaginationBar.vue,
    frontend/src/components/SkeletonLoader.vue,
    frontend/src/components/EmptyState.vue
  </read_first>
  <action>
Create `frontend/src/pages/SupportivePage.vue` following ProbationEndPage.vue layout exactly (per D-01).

**Template structure (per D-01 through D-13):**

1. **Breadcrumb** (per D-01): `<nav>` with Home icon, "/", "การนับเกื้อกูล" (per D-25, matching sidebar label)

2. **Page header**:
   - h1: "การนับเกื้อกูล"
   - p: "บันทึกวันเกื้อกูลต่อบุคคล"
   - Add button (top right): blue "เพิ่มรายการ" button with Plus icon, calls `openCreate()`

3. **Stat cards** (per D-02): 3 StatCard components:
   - "รายการทั้งหมด" with `FileText` icon, blue bg, value = pagination.total
   - "จำนวนบุคลากร" with `Users` icon, green bg, value = distinct personnel count (compute from rows)
   - "เพิ่มล่าสุด" with `Clock` icon, amber bg, value = count of records from current month (or "N/A" if unknown)

4. **Search bar** (per D-03): Debounced input with 300ms timeout, Thai IME composition guard using `compositionstart`/`compositionend` events to set an `isComposing` ref. Only call `fetchData()` when `!isComposing`. Placeholder: "ค้นหาชื่อ หรือสายงาน..."

5. **Loading/Error/Empty states** (per D-05): Same pattern as ProbationEndPage

6. **Data table** (per D-04, D-11, D-12):
   - Columns: ลำดับ | ชื่อ-สกุล | สายงานที่เกื้อกูล | วันเริ่มต้น | วันสิ้นสุด | จำนวนวัน | อัตราลดทอน | วันที่ได้ | จัดการ
   - Row values: `pagination.offset + index + 1` | `row.fullName` | `row.jobSeriesName` | `row.startDateThai` | `row.endDateThai` | `row.totalDays` | `row.ratioPercent + '%'` | `row.effectiveDays` | action buttons
   - Action buttons per D-12: Pencil icon (edit, `@click="openEdit(row)"`), Trash2 icon (delete, `@click="confirmDelete(row.supportiveId)"`)
   - In-table EmptyState when rows.length === 0 and !loading

7. **PaginationBar** (same pattern as ProbationEndPage):
   ```html
   <PaginationBar v-if="pagination.total > 0" :total="pagination.total" :limit="pagination.limit" :offset="pagination.offset" @update:offset="val => { pagination.offset = val; fetchData() }" />
   ```

8. **Create/Edit Modal** (per D-06, D-07, D-08, D-09, D-10, D-13):
   - Shown when `showModal` ref is true
   - Title: "เพิ่มรายการใหม่" (create) or "แก้ไขรายการ" (edit, when `editingRecord` is not null)
   - Modal overlay: `<div v-if="showModal" class="fixed inset-0 z-50 flex items-center justify-center">` with backdrop `<div class="fixed inset-0 bg-black/50" @click="closeModal"></div>` and white content div `class="relative bg-white rounded-lg shadow-xl w-full max-w-lg mx-4 p-6 max-h-[90vh] overflow-y-auto"`
   - Form fields (per D-13):
     - **personnel_id**: Label "บุคลากร", searchable text input with autocomplete. On input (debounced 300ms), call `api.get('/civil-servants?search=${encodeURIComponent(val)}&limit=10')`. Show dropdown list of results with `full_name` and `servant_id`. On select, set `formData.personnel_id = person.servant_id` and display `full_name` in input. Map `servant_id` to `personnel_id` (per Research pitfall 6). Disable in edit mode (show fullName as read-only text).
     - **primary_series_name**: Label "สายงานหลัก", text input `<input type="text" v-model="formData.primary_series_name">`
     - **job_series_name**: Label "สายงานที่เกื้อกูล", text input (per D-08, autocomplete from supportive_job_series if available, otherwise plain text input for v1.1)
     - **start_date**: Label "วันเริ่มต้น", `<input type="date" v-model="formData.start_date">` (per D-09)
     - **end_date**: Label "วันสิ้นสุด", `<input type="date" v-model="formData.end_date">` (per D-09)
     - **description**: Label "หมายเหตุ (ไม่บังคับ)", `<textarea v-model="formData.description" rows="3">`
   - Validation (per D-10): Required fields = personnel_id, primary_series_name, job_series_name, start_date, end_date. Show red border (`border-red-500`) and Thai error message below input when empty on submit. Set `formErrors` ref object with field keys.
   - Footer buttons: "ยกเลิก" (gray border, `@click="closeModal"`) and "บันทึก" (blue bg, `@click="handleSave"`)

9. **Delete Confirmation Dialog** (per D-12):
   - Shown when `showDeleteConfirm` ref is true
   - Simple modal: "ยืนยันการลบ" title, "คุณต้องการลบรายการนี้หรือไม่?" message
   - Buttons: "ยกเลิก" and "ลบ" (red bg)
   - On confirm: call `remove(deletingId)`, show toast, refresh data

**Script setup (per D-23, D-27):**

```javascript
import { ref, onMounted } from 'vue'
import { useSupportive } from '@/composables/useSupportive.js'
import { useApi } from '@/composables/useApi.js'
import { useUiStore } from '@/stores/ui.js'
// Import all needed components and icons
```

State refs:
- `loading`, `error`, `rows`, `pagination` (same pattern as ProbationEndPage)
- `searchQuery`, `isComposing` (Thai IME guard)
- `showModal`, `editingRecord`, `formData`, `formErrors` (modal state)
- `showDeleteConfirm`, `deletingId` (delete confirmation)
- `personnelSearch`, `personnelResults`, `showPersonnelDropdown` (autocomplete)
- `saving` (loading state for save button)

Functions:
- `fetchData()` -- calls `fetchList({ search, limit, offset })`, maps to rows/pagination
- `onSearchInput()` -- 300ms debounce, skip if isComposing (per D-03)
- `openCreate()` -- reset formData, set editingRecord=null, showModal=true
- `openEdit(record)` -- populate formData from record, set editingRecord=record, showModal=true
- `closeModal()` -- showModal=false, clear formErrors
- `handleSave()` -- validate, call create/update, show toast (per D-27), close modal, fetchData
- `confirmDelete(id)` -- set deletingId, showDeleteConfirm=true
- `handleDelete()` -- call remove(deletingId), show toast, fetchData
- `onPersonnelInput()` -- debounced 300ms autocomplete search
- `selectPersonnel(person)` -- set formData.personnel_id = person.servant_id, personnelSearch = person.full_name

Toast messages (per D-27): use `useUiStore().showToast('message', 'type')`:
- Create success: `showToast('บันทึกสำเร็จ', 'success')`
- Update success: `showToast('อัปเดตสำเร็จ', 'success')`
- Delete success: `showToast('ลบสำเร็จ', 'success')`
- Error: `showToast(err.message || 'เกิดข้อผิดพลาด', 'error')`

formData initial shape:
```javascript
const defaultFormData = () => ({
  personnel_id: null,
  primary_series_name: '',
  job_series_name: '',
  start_date: '',
  end_date: '',
  description: '',
})
```
  </action>
  <verify>
    <automated>grep -c "การนับเกื้อกูล" frontend/src/pages/SupportivePage.vue && grep -c "useSupportive" frontend/src/pages/SupportivePage.vue && grep -c "showModal" frontend/src/pages/SupportivePage.vue && grep -c "showDeleteConfirm" frontend/src/pages/SupportivePage.vue && grep -c "isComposing" frontend/src/pages/SupportivePage.vue && grep -c "civil-servants" frontend/src/pages/SupportivePage.vue && grep -c "PaginationBar" frontend/src/pages/SupportivePage.vue</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/pages/SupportivePage.vue exists and is >= 200 lines
    - grep "การนับเกื้อกูล" returns match (Thai page title per D-25)
    - grep "useSupportive" returns match (composable import per D-23)
    - grep "showModal" returns match (modal dialog per D-06)
    - grep "showDeleteConfirm" returns match (delete confirmation per D-12)
    - grep "isComposing" returns match (Thai IME guard per D-03)
    - grep "civil-servants" returns match (personnel autocomplete per D-07)
    - grep "PaginationBar" returns match (pagination per D-01)
    - grep "StatCard" returns match (stat cards per D-02)
    - grep "SkeletonLoader" returns match (loading state per D-05)
    - grep "EmptyState" returns match (empty state per D-05)
    - grep "showToast" returns match (toast notifications per D-27)
    - grep "Pencil" returns match (edit button icon per D-12)
    - grep "Trash2" returns match (delete button icon per D-12)
    - grep "border-red-500" returns match (form validation error style per D-10)
    - grep "startDateThai\|start_date_thai" returns match (Buddhist Era dates per D-26)
    - grep "ratioPercent\|ratio_percent" returns match (ratio column per D-11)
    - grep "effectiveDays\|effective_days" returns match (effective days column per D-11)
  </acceptance_criteria>
  <done>SupportivePage.vue exists with full CRUD: list with 8 columns, stat cards, search with IME guard, pagination, modal create/edit with personnel autocomplete and validation, delete confirmation, toast notifications</done>
</task>

</tasks>

<verification>
- SupportivePage.vue follows ProbationEndPage layout pattern
- All D-01 through D-13 decisions implemented
- Personnel autocomplete calls /civil-servants?search= API
- Form validation with red border + Thai error messages
- Delete confirmation dialog before removing records
- Buddhist Era dates displayed in table
</verification>

<success_criteria>
- HR can view supportive experience records in a paginated table
- HR can create new records via modal form with personnel autocomplete
- HR can edit existing records via modal form
- HR can delete records with confirmation dialog
- Search works with Thai IME composition guard
- All text in Thai (per D-25)
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-crud-pages/06-02-SUMMARY.md`
</output>
