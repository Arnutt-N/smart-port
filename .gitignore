# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
frontend/node_modules/
project-bolt/node_modules/

# Build outputs
dist
dist-ssr
build
*.local

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
Thumbs.db
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~

# Uploads และ generated files
uploads/*
!uploads/.gitkeep
backend/uploads/*
!backend/uploads/.gitkeep
database/export-*.sql
database/reimport-data.sql

# Rate limiter runtime state (file-based sliding window) — local เท่านั้น
backend/storage/rate_limits/*
!backend/storage/rate_limits/.gitkeep

# Test coverage
coverage/
.nyc_output/

# Playwright
frontend/playwright-report/
frontend/test-results/
frontend/blob-report/
frontend/playwright/.cache/

# Project specific
project-bolt
admin-dashboard-integration-test.html
smartport/

# Backup files
*.backup
*.bak
*.orig

# Cache directories
.cache/
.npm/
.yarn/

# PHP / Composer / PHPUnit
backend/vendor/
backend/.phpunit.cache/
backend/.phpunit.result.cache

secrets/

# Session/debug artifacts — ห้าม track (diff dumps, crash dumps, debug screenshots)
*diff.txt
*.stackdump
/probation-check.png

# Local agent/tool dirs (config/session — อาจมีข้อมูล local ที่ไม่ควร public)
.claude/
.kilo/
.playwright-cli/
.playwright-mcp/

# Session logs (prompts/paths/เนื้อหา session — local เท่านั้น)
project-log-md/

# Binary reference assets — เก็บ local เท่านั้น (ไฟล์ใหญ่/สื่อ ไม่เข้า git; ถ้าต้องแชร์ให้ใช้ LFS หรือ storage ภายนอก)
docs/*.pdf
docs/documents/
docs/images/
docs/scrennshorts/
docs/project-documents/*.docx
